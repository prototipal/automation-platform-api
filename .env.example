# Application Configuration
NODE_ENV=development
PORT=3008
GLOBAL_PREFIX=api

# PostgreSQL Database Configuration (Application DB)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=your_postgres_username
DB_PASSWORD=your_postgres_password
DB_NAME=automation_platform
DB_SSL=false
DB_CONNECTION_LIMIT=10

# Docker Database Configuration
DATABASE_HOST=app-db
DATABASE_PORT=5432
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=postgres
DATABASE_NAME=automation_platform

# Nginx Proxy Manager Database Configuration
NPM_DB_ROOT_PASSWORD=npm123
NPM_DB_NAME=npm
NPM_DB_USER=npm
NPM_DB_PASSWORD=npm123

# Additional Database Settings
# Set to true for production deployments with SSL
# DB_SSL=true

# Replicate API Configuration
REPLICATE_API_TOKEN=your_replicate_api_token_here

# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key
SUPABASE_BUCKET_NAME=generations
SUPABASE_MAX_FILE_SIZE_MB=50

# Supabase Storage Tips:
# 1. Make sure your SUPABASE_URL is accessible from your deployment environment
# 2. Verify the 'generations' bucket exists in your Supabase project
# 3. Ensure bucket policies allow public read access for uploaded files
# 4. The service role key should have full access to storage operations
# 5. For production, consider setting up proper RLS policies

# Authentication Configuration
STATIC_AUTH_TOKEN=your_static_auth_token_here
PROFIT_MARGIN=1.5
CREDIT_VALUE_USD=0.05

# Example Production Settings:
# NODE_ENV=production
# DB_HOST=your-production-db-host
# DB_USERNAME=your-production-username
# DB_PASSWORD=your-secure-production-password
# DB_SSL=true